Github and Remote Repositories:
1. git remote add origin https://github.com/yuliussetyawan/git_demo.git
2. git branch -M main
3. git push -u origin main(origin is the name of the remote, main is the name of the branch)



